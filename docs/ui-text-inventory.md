# UI English Text Inventory

This inventory covers user-visible English text found in `src/App.tsx`, `src/components/**/*.tsx`, `src/lib/**/*.ts`, `src/data/**/*.ts`, and `src/styles/**/*.css` that is not yet consistently routed through the global `t()` helper.

Already-routed language, top bar, tour marker, and tutorial-page copy in `src/lib/i18n/translations.ts` is not repeated here unless the surrounding component still has separate hardcoded English text. Rows group repeated patterns where multiple components use the same visible copy style.

| File | Component / Area | Current English UI text | Move to `translations.ts`? | Suggested key area | Notes |
|---|---|---|---|---|---|
| `src/App.tsx` | Autosave status | `Autosave unavailable`; `Autosave ready`; `Restored local workspace {time}`; `Local workspace saved {time}`; `Input imported. Graph not generated yet.`; `Imported workspace {time}`; `Full AI Context imported as workspace`; `Local saved workspace cleared. Current open workspace is unchanged.` | yes | `app.autosave.*` | Displayed in the top bar through `autosaveStatus`. |
| `src/App.tsx` | Clear workspace confirm | `Clear the locally saved EFlow workspace? Current open workspace will stay unchanged.` | yes | `app.confirm.clearLocalWorkspace` | Browser confirm text. |
| `src/App.tsx` | Audit actor label | `Local user` | yes | `audit.actor.localUser` | Appears in audit event records and may surface in history/export. |
| `src/App.tsx` | Audit summaries | `Generated engineering graph with {nodeCount} nodes and {edgeCount} edges.`; `Replaced engineering graph with {nodeCount} nodes and {edgeCount} edges.`; `Imported EngineeringFlowInput "{projectName}".`; `Imported {sourceLabel} "{projectName}" with {nodeCount} nodes and {edgeCount} edges.`; `Changed node "{title}" review status from {from} to {to}.`; `Changed node "{title}" lifecycle status from {from} to {to}.`; `Changed edge "{id}" review status from {from} to {to}.`; `Changed edge "{id}" lifecycle status from {from} to {to}.` | yes | `audit.summary.*` | Keep dynamic project/node/edge/status values as interpolations. |
| `src/components/layout/CollapsibleSection.tsx` | Section toggle aria label | `Collapse {title}`; `Expand {title}` | yes | `common.disclosure.*` | Accessibility text. |
| `src/components/aiChat/AIChatConsole.tsx` | Header | `AI collaboration`; `AI Collaboration Console`; `Chat with your model using optional current EFlow Context.` | yes | `aiChat.header.*` | Main console title/subtitle. |
| `src/components/aiChat/AIChatConsole.tsx` | Boundary badges | `AI collaboration boundaries`; `Personal BYOK`; `Draft only`; `No graph mutation` | yes | `aiChat.boundaries.*` | Preserve `BYOK` if treated as product terminology. |
| `src/components/aiChat/AIChatConsole.tsx` | Collapse control | `Expand`; `Collapse` | yes | `common.disclosure.*` | Shared with collapsible sections. |
| `src/components/aiChat/AIChatConsole.tsx` | Provider fields | `Base URL`; `Model`; `API key`; `Remember key in this browser`; `sk-...` | yes | `aiChat.settings.*` | `sk-...` may stay as a placeholder example if desired. |
| `src/components/aiChat/AIChatConsole.tsx` | Provider helper text | `API key is used only in this browser session unless remembered. Remembered keys are stored in localStorage as eflow.aiChat.apiKey.`; `Model responses are drafts and cannot modify EFlow. Use Local Command Import to validate, dry-run, and apply any generated command.` | yes | `aiChat.settings.helper.*` | Keep storage key and product terms literal. |
| `src/components/aiChat/AIChatConsole.tsx` | Prompt/context labels | `Prompt mode`; `Context mode`; `chars`; `rough tokens` | yes | `aiChat.prompt.*`; `aiChat.context.*` | Small but prominent configuration labels. |
| `src/components/aiChat/AIChatConsole.tsx` | Context mode helper text | `No context selected. Only your message and prompt-mode instructions will be sent.`; `No Full EFlow Context available yet. Generate or import a graph first.`; `No context: sends only your message and prompt-mode instructions.`; `Summary: sends compact project state.`; `Full: sends the complete eflow-context/v0.1 JSON on every message while selected.`; `Full context may use significantly more tokens.`; `Summary context sends compact project state, not the full context JSON.`; `Full context may use significantly more tokens. The complete eflow-context/v0.1 JSON is sent on every message while Full EFlow Context is selected.`; `Full EFlow Context is selected, but no graph context is available yet. This message will send without EFlow Context JSON.` | yes | `aiChat.context.helper.*`; `aiChat.context.warning.*` | High priority because it explains token/cost behavior. Keep schema string literal. |
| `src/components/aiChat/AIChatConsole.tsx` | Empty and pending states | `Draft with an external model while EFlow stays local-first.`; `Attach context only when you want the current graph snapshot sent to the configured provider.`; `Assistant`; `Thinking`; `Waiting for provider response...` | yes | `aiChat.state.*`; `aiChat.message.*` | Visible in chat transcript. |
| `src/components/aiChat/AIChatConsole.tsx` | Composer | `Ask for a Codex prompt, paste a report, or explore an architecture decision.`; `AI collaboration message` | yes | `aiChat.composer.*` | Placeholder and aria label. |
| `src/components/aiChat/AIChatConsole.tsx` | Actions | `Send`; `Sending`; `Copy composed prompt preview`; `Copy latest response`; `Clear chat`; `Clear API key` | yes | `aiChat.actions.*` | Core button labels. |
| `src/components/aiChat/AIChatConsole.tsx` | Status messages | `Write a message before sending.`; `Add an API key before sending.`; `Add a model name before sending.`; `Add a valid provider base URL before sending.`; `Response received.`; `Response received with {count} context characters attached.`; `Latest assistant response copied.`; `Composed prompt preview copied.`; `Chat cleared. No graph state changed.`; `API key cleared from this session and localStorage.` | yes | `aiChat.status.*`; `aiChat.error.*` | User-facing validation and success copy. |
| `src/components/aiChat/AIChatConsole.tsx` | Clipboard/provider errors | `Clipboard API is unavailable.`; `Could not copy the latest response.`; `Could not copy the composed prompt preview.`; `Network or CORS error while contacting the provider. Some providers block direct browser API calls.`; `Provider returned HTTP {status}{statusText}.`; `Provider returned an empty response.`; `Provider response did not include output_text or a recognized content shape.`; `Unexpected provider error.` | yes | `aiChat.error.*` | Technical terms can remain literal inside translated sentences. |
| `src/components/aiChat/AIChatConsole.tsx` | Prompt preview headings | `SYSTEM PROMPT`; `USER PROMPT` | yes | `aiChat.preview.*` | Copy-preview headings, not model instructions. |
| `src/lib/buildAIChatPrompt.ts` | Prompt mode labels | `Free chat`; `Generate Codex milestone prompt`; `Convert Codex report to eflow-command`; `Raw idea to EngineeringFlowInput JSON`; `Progress / handoff summary`; `Strategic architecture consultation` | yes | `aiChat.promptMode.*` | Visible in prompt mode select. |
| `src/lib/buildAIChatPrompt.ts` | Context mode labels | `No context`; `Summary context`; `Full EFlow Context` | yes | `aiChat.contextMode.*` | Visible in context mode select. |
| `src/lib/buildAIChatPrompt.ts` | Context summary preview | `Graph state: no graph has been generated or imported yet.`; `Project: {name}`; `Intent: {intent}`; `Input shape: ...`; `Graph state: {nodes} nodes and {edges} edges.`; `Node review state: ...`; `Edge review state: ...`; `Blocking questions: ...`; `Top blocking questions:`; `CURRENT EFLOW CONTEXT SUMMARY:`; `This is compact project state, not the full context JSON.`; `CURRENT EFLOW CONTEXT JSON ({chars} chars):`; `User message:` | yes | `aiChat.promptPreview.*` | Visible when copying composed prompt preview. Keep context block labels stable if downstream tests rely on them. |
| `src/lib/buildAIChatPrompt.ts` | Model-facing prompt instructions | `You are collaborating inside EFlow...`; `EngineeringFlowGraph is the source of truth...`; `Mode: Free chat.`; `Mode: Generate Codex milestone prompt.`; `Output valid EngineeringFlowInput JSON only. Do not output markdown.`; other mode instruction paragraphs | no | `aiChat.promptTemplate.*` | Not UI chrome. Consider separate prompt localization only if the product wants localized model instructions. |
| `src/components/command/LocalCommandImportPanel.tsx` | Panel heading and badges | `Local command`; `AI command import`; `Graph ready`; `Validation only` | yes | `commandImport.header.*` | Main AI command panel. |
| `src/components/command/LocalCommandImportPanel.tsx` | Helper text | `Paste an eflow-command/v0.1 JSON command to validate, dry-run, or apply local graph updates. This stays in the browser and only changes the current graph after Apply Command.`; `Accepted schema:`; `Recommended workflow: Validate -> Dry Run -> Apply. reviewStatus updates confirmation state; lifecycleStatus updates implementation progress.`; `Generate or import an engineering graph before dry-running or applying commands.` | yes | `commandImport.helper.*` | Preserve schema/status field names. |
| `src/components/command/LocalCommandImportPanel.tsx` | Actions and inputs | `Insert example command`; `Clear`; `Local AI command JSON`; `Validate`; `Dry Run`; `Apply Command`; `{"schemaVersion":"eflow-command/v0.1","operations":[...]}` | yes | `commandImport.actions.*`; `commandImport.input.*` | JSON placeholder can remain untranslated if treated as an example. |
| `src/components/command/LocalCommandImportPanel.tsx` | Status messages | `Example command targets node "{id}".`; `Example inserted with a placeholder node id. Generate or import a graph before dry-run/apply.`; `Command is valid.`; `Dry run completed. Graph state was not changed.`; `Dry run failed. Graph state was not changed.`; `Generate or import an engineering graph before dry-running commands.`; `Generate or import an engineering graph before applying commands.`; `Applied AI command {id} with {count} operation(s).`; `Command applied to the current graph.`; `Command mode is dry_run. Graph state was not changed.`; `Command failed. Graph state was not changed.`; `Invalid JSON.`; `Invalid JSON. {message}`; `Command validation failed. Graph state was not changed.` | yes | `commandImport.status.*`; `commandImport.error.*` | User-visible status/validation copy. |
| `src/components/command/LocalCommandImportPanel.tsx` | Result labels | `errors`; `warnings`; `Errors`; `Warnings`; `changes`; `Apply errors`; `Apply warnings`; `Changes` | yes | `commandImport.results.*` | Repeated section labels. |
| `src/components/export/CopyJsonButton.tsx` | Copy button states | `Copied`; `{label} fallback JSON` | yes | `common.copied`; `export.copy.fallbackLabel` | Aria/fallback UI text. |
| `src/components/export/EFlowContextExportPanel.tsx` | Panel heading | `AI-Native Context Export`; `EFlow Context v0.1`; `Machine-readable project context for downstream AI agents. This is the preferred AI read format, distinct from visual exports.` | yes | `export.aiContext.header.*` | Keep schema version/product term literal. |
| `src/components/export/EFlowContextExportPanel.tsx` | Metadata labels | `schema`; `project`; `nodes`; `edges`; `blocking questions`; `completed nodes`; `Accepted command schema:` | yes | `export.aiContext.meta.*` | Lowercase labels are visible in stat grid. |
| `src/components/export/EFlowContextExportPanel.tsx` | Guidance/actions | `Downstream agents should write commands through Local Command Import. reviewStatus maps to confirmation state; lifecycleStatus remains implementation progress.`; `Copy AI Context JSON`; `Download AI Context JSON`; `Generate or import an engineering graph to export AI-native context.` | yes | `export.aiContext.*` | Preserve command/schema field names. |
| `src/components/export/JsonExportPanel.tsx` | Legacy JSON export | `Export`; `AI-readable JSON`; `Copy EngineeringFlowInput JSON`; `Copy EngineeringFlowGraph JSON`; `Copy Full AI Context JSON` | yes | `export.json.*` | Preserve schema names. |
| `src/components/export/WorkspacePersistencePanel.tsx` | Status messages | `Workspace JSON downloaded.`; `Workspace import rejected`; `Workspace JSON imported.`; `Full AI Context imported as an editable workspace.`; `EngineeringFlowInput imported. Generate Engineering Flow when ready.`; `{count} more issue(s).` | yes | `workspacePersistence.status.*`; `workspacePersistence.error.*` | Import/export feedback. |
| `src/components/export/WorkspacePersistencePanel.tsx` | Panel text and actions | `Workspace`; `Resume locally`; `Workspace JSON restores EFlow editing and review state. Full AI Context remains the AI handoff format.`; `Copy Workspace JSON`; `Download Workspace JSON`; `Import workspace / context`; `Paste Workspace JSON, Full AI Context, or EngineeringFlowInput`; `Import`; `Clear local workspace` | yes | `workspacePersistence.*` | High priority persistence UI. |
| `src/components/export/WorkspacePersistencePanel.tsx` | JSON placeholder | `{"schemaVersion":"eflow-workspace/v0.3", ...}` | no | n/a | Example JSON/schema text. |
| `src/components/graph/EngineeringFlowCanvas.tsx` | Graph type filters | `All`; `Intent`; `Users`; `Screens`; `Features`; `Flow`; `Data`; `AI`; `Questions`; `Confirmed`; `Needs Review` | yes | `canvas.filters.graph.*` | Visible filter chips. |
| `src/components/graph/EngineeringFlowCanvas.tsx` | Empty canvas | `Canvas`; `Generate an engineering flow`; `The graph will render here from structured input. It stays editable, but the exported JSON remains the source of truth.` | yes | `canvas.empty.*` | Empty state. |
| `src/components/graph/EngineeringFlowCanvas.tsx` | Canvas header | `Engineering Flow Canvas`; `{visible} of {total} nodes / {visible} of {total} edges`; `Canvas filters`; `Graph`; `Graph filters`; `Lifecycle`; `Lifecycle filters` | yes | `canvas.header.*`; `canvas.filters.*` | Main canvas UI and aria labels. |
| `src/components/graph/EngineeringNodeCard.tsx` | Node card status/type display | Node type enum values; review status enum values; lifecycle status enum values | yes | `graph.nodeType.*`; `review.status.*`; `lifecycle.status.*` | Move display labels to translations while preserving raw schema values. |
| `src/components/lifecycle/LifecycleProgressSummary.tsx` | Lifecycle labels | `Draft`; `Planned`; `Ready`; `Developing`; `Completed`; `Blocked`; `Needs refactor`; `Deprecated` | yes | `lifecycle.status.*` | Shared across filters, inspectors, and summaries. |
| `src/components/lifecycle/LifecycleProgressSummary.tsx` | Lifecycle summary | `Lifecycle summary`; `Implementation Progress`; `{nodes} nodes / {edges} edges`; `Implementation lifecycle counts`; `Lifecycle`; `Nodes`; `Edges` | yes | `lifecycle.summary.*` | Progress table and aria label. |
| `src/components/inspector/InspectorPanel.tsx` | Section titles | `Selected Item`; `Project`; `Review`; `Implementation Progress`; `Manual Editing`; `Change History`; `AI Interop`; `Workspace / Export` | yes | `inspector.sections.*` | Right panel navigation. |
| `src/components/inspector/InspectorPanel.tsx` | Section subtitles/meta | `Node`; `Edge`; `Inspect and edit the selected graph item.`; `Review the generated graph summary.`; `Confirm generated nodes and relationships.`; `Queue`; `Lifecycle progress and manual context.`; `Live`; `Add missing graph nodes and relationships.`; `Local`; `Local workspace audit history.`; `{count} events`; `Local command intake and AI-native context.`; `JSON`; `Workspace persistence and legacy JSON handoffs.`; `Files`; `Project summary before graph generation.`; `Validate local commands and prepare AI context.`; `Import, restore, and export workspace JSON.` | yes | `inspector.sections.*` | Includes collapsible-section labels. |
| `src/components/inspector/NodeInspector.tsx` | Node inspector heading/actions | `Node inspector`; `Manual context`; `Confirm node`; `Mark needs review` | yes | `inspector.node.*` | Selected node panel. |
| `src/components/inspector/NodeInspector.tsx` | Node fields | `Type`; `Status`; `Lifecycle status`; `Title`; `Description`; `AI-readable summary`; `Advanced provenance`; `Confidence`; `Source type`; `Source section`; `Source input ID`; `Generation rule`; `none` | yes | `inspector.node.fields.*`; `inspector.provenance.*` | Preserve raw IDs and enum values. |
| `src/components/inspector/EdgeInspector.tsx` | Edge inspector heading/actions | `Edge inspector`; `Manual relationship`; `Confirm edge`; `Reject edge` | yes | `inspector.edge.*` | Selected edge panel. |
| `src/components/inspector/EdgeInspector.tsx` | Edge fields | `Relationship type`; `Status`; `Lifecycle status`; `Description`; `Advanced provenance`; `Confidence`; `Source type`; `Source input IDs`; `Generation rule`; `none` | yes | `inspector.edge.fields.*`; `inspector.provenance.*` | Preserve raw relationship enum values unless display labels are added. |
| `src/components/inspector/EmptyInspector.tsx` | Project summary | `Project summary`; `No intent has been written yet.`; `Total nodes`; `Confirmed nodes`; `Suggested nodes`; `Needs review nodes`; `Total edges`; `Confirmed edges`; `Suggested edges`; `Rejected edges`; `Blocking questions` | yes | `inspector.projectSummary.*` | Shown before/without a selected item. |
| `src/components/review/ReviewPanel.tsx` | Queue controls/states | `Next unreviewed`; `Selected item may be hidden by the current canvas filter.`; `No review items match this filter.`; `{count} more items in this filter.` | yes | `review.queue.*` | Review workflow copy. |
| `src/components/review/ReviewProgress.tsx` | Progress labels | `Review mode`; `Review progress`; `Suggested:`; `Confirmed:`; `Needs review:`; `Rejected:` | yes | `review.progress.*` | Summary labels. |
| `src/components/review/ReviewItemCard.tsx` | Item card | `Manual context`; `Manual relationship`; `Confidence {value}`; `Select`; `Confirm`; `Needs review`; `Reject` | yes | `review.item.*`; `review.status.*` | Preserve dynamic item details. |
| `src/components/review/ReviewQueueFilters.tsx` | Filter chips | `Needs action`; `Critical`; `Nodes`; `Edges`; `Confirmed`; `Needs review`; `Rejected`; `All`; `Review queue filters` | yes | `review.filters.*` | Includes aria label. |
| `src/components/audit/AuditLogPanel.tsx` | Audit panel | `Audit Log`; `Change History`; `{count} events`; `No changes recorded yet.` | yes | `auditLog.*` | History panel. |
| `src/components/audit/AuditLogPanel.tsx` | Event/source labels | `Graph generated`; `Graph replaced`; `Workspace imported`; `Input imported`; `Full context imported`; `Manual node created`; `Manual edge created`; `AI command applied`; `Node review changed`; `Node lifecycle changed`; `Edge review changed`; `Edge lifecycle changed`; `Manual UI`; `AI command`; `Workspace`; `System` | yes | `auditLog.eventType.*`; `auditLog.source.*` | Translate labels, not raw event type identifiers. |
| `src/components/input/StructuredInputPanel.tsx` | Dirty state | `Input changed. Generate again to refresh graph.` | yes | `input.status.dirty` | Prominent workflow warning. |
| `src/components/input/ProjectIntentEditor.tsx` | Project intent | `Project`; `Intent`; `Project name`; `Name this engineering flow`; `Project intent`; `Describe what this product is trying to make possible.` | yes | `input.projectIntent.*` | First structured input section. |
| `src/components/input/*Editor.tsx` | Shared editor UI | `{count} items`; `Add`; `Delete`; `No items yet.` | yes | `input.common.*` | Repeated across editor components. |
| `src/components/input/UserTypesEditor.tsx` | User types | `User types`; `New user type`; `Name`; `Goal`; `Description` | yes | `input.userTypes.*` | New item defaults are visible after adding. |
| `src/components/input/ScreensEditor.tsx` | Screens | `Main screens`; `New screen`; `Name`; `Purpose`; `Key actions`; `Action` | yes | `input.screens.*` | `Action` is the key-action placeholder. |
| `src/components/input/CoreFunctionsEditor.tsx` | Core functions | `Core functions`; `New function`; `Name`; `Description`; `Priority`; `Related screen IDs`; `Related data object IDs` | yes | `input.coreFunctions.*` | Priority enum display values should get labels separately. |
| `src/components/input/FlowStepsEditor.tsx` | Flow steps | `Flow steps`; `New flow step`; `Step`; `Title`; `Description`; `Related screen ID`; `Related function IDs` | yes | `input.flowSteps.*` | ID placeholders can stay raw examples. |
| `src/components/input/DataObjectsEditor.tsx` | Data objects | `Data objects`; `NewDataObject`; `Name`; `Description` | yes | `input.dataObjects.*` | Consider changing default to a localized display string while keeping IDs stable. |
| `src/components/input/AiRolesEditor.tsx` | AI roles | `AI roles`; `New AI task`; `Task`; `Requires human confirmation`; `Related screen IDs`; `Related function IDs` | yes | `input.aiRoles.*` | Keep `AI` literal if product terminology. |
| `src/components/input/UnknownsEditor.tsx` | Unknowns | `Unknowns`; `New open question`; `Question`; `Blocks generation decisions`; `Related object IDs` | yes | `input.unknowns.*` | Blocking-question form. |
| `src/components/input/StringListEditor.tsx` | List editor | `Add item`; `Add`; `No items yet.` | yes | `input.common.list.*` | Reusable list controls. |
| `src/components/input/ImportInputJson.tsx` | Import input JSON | `Expected schemaVersion "engineering-flow-input/v0".`; `JSON is missing required EngineeringFlowInput fields.`; `EngineeringFlowInput JSON loaded.`; `Could not import JSON.`; `Import input JSON`; `Paste an EngineeringFlowInput object`; `Import`; `{"schemaVersion":"engineering-flow-input/v0", ...}` | yes | `input.importJson.*` | Preserve schema names and JSON placeholder. |
| `src/components/graphEditing/ManualContextSummary.tsx` | Manual context summary | `Suggested`; `Confirmed`; `Needs review`; `Rejected`; `Manual Context`; `{count} manual items`; `Manual nodes`; `Manual relationships`; `No manual context added yet.`; `Nodes by review status`; `Relationships by review status` | yes | `manualContext.*`; `review.status.*` | Shared review status labels. |
| `src/components/graphEditing/AddManualNodePanel.tsx` | Manual node panel | `Manual graph edit`; `Add manual node`; `Graph ready`; `Node type`; `Lifecycle status`; `Manual feature title`; `What this node represents in the engineering graph`; `AI-readable summary`; `Optional concise summary for downstream AI context`; `Review status`; `Add node` | yes | `manualNode.*` | Form labels and placeholders. |
| `src/components/graphEditing/AddManualNodePanel.tsx` | Manual node status | `Add a title before creating a manual node.`; `Add a description so exports have enough context for the new node.`; `Added manual node "{title}".`; `Added "{title}" and selected it.` | yes | `manualNode.status.*` | Validation and success messages. |
| `src/components/graphEditing/AddManualEdgePanel.tsx` | Manual edge panel | `Manual graph edit`; `Add manual edge`; `Graph ready`; `Needs nodes`; `At least two nodes are required before a manual relationship can be added.`; `Source node`; `Target node`; `Relationship type`; `Lifecycle status`; `Description`; `Why these two graph nodes are related`; `Review status`; `Add edge` | yes | `manualEdge.*` | Form labels and placeholders. |
| `src/components/graphEditing/AddManualEdgePanel.tsx` | Manual edge status | `Add at least two nodes before creating a manual edge.`; `Choose a source node before creating an edge.`; `Choose a target node before creating an edge.`; `Choose two different nodes before creating an edge.`; `Add a description so exports have enough context for the new edge.`; `Added manual edge "{source}" to "{target}".`; `Added manual edge and selected it.` | yes | `manualEdge.status.*` | Validation and success messages. |
| `src/lib/applyEflowCommand.ts` | Command apply errors | Existing-node/edge lookup failures; unsupported node/edge type messages; invalid transition messages; duplicate edge messages | yes | `commandApply.error.*` | These are surfaced by Local Command Import. Preserve operation names, IDs, and schema paths. |
| `src/lib/applyEflowCommand.ts` | Command apply change summaries | `Updated node...`; `Transitioned node...`; `Upserted node...`; `Updated edge...`; `Transitioned edge...`; `Upserted edge...`; skipped/unchanged summaries | yes | `commandApply.change.*` | Shown in dry-run/apply results. |
| `src/lib/eflowCommandValidation.ts` | Command validation | `Command envelope must be an object.`; `schemaVersion must be "{version}".`; messages for `graphId`, `intent`, `mode`, `operations`, `op`, `node`, `edge`, `patch`, `reviewStatus`, `lifecycleStatus`, `position`, `provenance`, `decision`, `question`, and non-empty string checks | yes | `validation.command.*` | Translate message templates while preserving JSON path fragments and enum values. |
| `src/lib/workspaceValidation.ts` | Workspace validation | `Workspace document must be an object.`; workspace schema/version/app/saved-at/name messages; `Workspace engineeringFlowInput is missing required fields.`; `Graph must be an object.`; graph schema/nodes/edges messages; node and edge missing/invalid field messages | yes | `validation.workspace.*` | Shown in workspace/context import errors. |
| `src/lib/auditLog.ts` | Audit log validation | Audit event object/schema/version/source/event/actor/target validation messages | yes | `validation.audit.*` | May surface during workspace import. |
| `src/lib/manualGraphEditing.ts` | Manual graph validation | `Node "{id}" already exists.`; `Edge "{id}" already exists.`; `Edge source node "{id}" does not exist.`; `Edge target node "{id}" does not exist.`; `An edge already exists for {source} -> {target} ({relationshipType}).` | yes | `validation.manualGraph.*` | Preserve IDs and relationship type. |
| `src/lib/generateEngineeringFlow.ts` | Generated provenance | `Generated from structured input and not yet human-confirmed.`; `Generated because the project intent serves this user type: '{name}'.`; `Generated because the project intent contains this main screen: '{name}'.`; `Generated because flow step {from} leads to flow step {to}.`; `Generated because feature '{name}' declares relatedScreenIds including '{id}'.`; `Generated because feature '{name}' declares relatedDataObjectIds including '{id}'.`; `Generated because flow step {step} declares relatedScreenId '{id}'.`; `Generated because flow step {step} declares relatedFunctionIds including '{id}'.`; `Generated because AI role '{task}' requires human confirmation for this related function: '{id}'.`; `Generated because AI role '{task}' requires human confirmation for this related screen: '{id}'.`; `Generated because unknown '{question}' declares relatedObjectIds including '{id}'.` | yes | `graph.provenance.*` | Visible in review/inspector/export provenance. |
| `src/lib/buildReviewQueue.ts` | Generated review copy | Review item title/subtitle/reason templates including manual-context and not-human-confirmed messages | yes | `review.generated.*` | Translate display templates while preserving item values. |
| `src/lib/buildEflowContextExport.ts` | Export context descriptions | `Engineering flow context for {projectName}.`; `EngineeringFlowGraph does not yet have a decision record store.`; `EngineeringFlowGraph does not yet have a standalone question record store.`; accepted workflow descriptions; guardrail descriptions; Mermaid fallback `No explicit dependency edges`; confirmation policy descriptions; lifecycle policy descriptions | no | `export.aiContext.generated.*` | AI-native export payload, not UI chrome. Move only if the product wants localized exported context. |
| `src/data/emptyEngineeringFlowInput.ts` | Default project | `Untitled Engineering Flow` | yes | `input.defaults.projectName` | Visible default project name. |
| `src/data/todoThoughtUniverseExample.ts` | Example project and seed data | `Todo Thought Universe / ...`; `AI Assistant`; `Universe Dashboard`; `Quick Capture`; `Idea Inbox`; `Todo Universe Board`; `Project Detail`; `Relationship Map`; `AI Planning Panel`; `Export / Engineering Handoff`; `ThoughtItem`; `Universe`; `TodoItem`; `Project`; `Relationship`; `AIInsight`; `EngineeringHandoff`; other English example names/descriptions embedded in the seed | yes | `example.todoThoughtUniverse.*` | Example/demo content can move after core UI. Keep product/schema object names if intentionally technical. |
| `src/styles/**/*.css` | CSS-generated content | No hardcoded English UI strings found. `src/styles/tutorial.css` uses `content: attr(data-tour-step)` and `content: attr(data-tour-title) "\A" attr(data-tour-body);` | no | n/a | Tour text comes from component attributes, many already routed through `t()`. |

## Likely Product Terms To Keep In English

- `EFlow`
- `Engineering Flow Intake`
- `EngineeringFlowInput`
- `EngineeringFlowGraph`
- `EFlow Context`
- `Full AI Context`
- `AI-native context`
- `eflow-context/v0.1`
- `eflow-command/v0.1`
- `Workspace JSON`
- `API key`
- `Base URL`
- `Model`
- `BYOK`
- `JSON`
- `Codex`
- Raw schema fields and enum values when shown as data, including `reviewStatus`, `lifecycleStatus`, `schemaVersion`, `dry_run`, `apply`, operation names, IDs, and JSON paths.

## High-Priority UI Areas To Move First

1. AI Collaboration Console context-mode, token/cost, provider error, and action text.
2. Structured Input Panel labels, placeholders, and default new-item names.
3. Canvas, inspector, review queue, lifecycle, and status display labels.
4. Workspace import/export and Local Command Import validation/status messages.
5. Audit log event labels and summaries.
6. Generated provenance and validation templates that appear inside review/inspector/import flows.
7. Example seed data after the core workflow UI has translation coverage.
